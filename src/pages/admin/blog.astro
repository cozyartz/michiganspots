---
import Layout from '../../layouts/Layout.astro';
import { Card } from '../../components/Card';
import { BlogManagement } from '../../components/BlogManagement';

// TODO: Add authentication check - ensure user is admin
const userEmail = 'admin@michiganspots.com'; // From session
const userName = 'Admin'; // From session
---

<Layout
  title="Blog Management - Michigan Spots Admin"
  description="Manage blog posts and import from NeuronWriter"
>
  <div class="container mx-auto px-4 py-20">
    <BlogManagement client:load userEmail={userEmail} userName={userName} />
  </div>
</Layout>
